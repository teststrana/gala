. git clone https://github.com/teststrana/gala.git

cd gala 

###
First

run in terminal 

./compile.sh --skip-desktop

###
Secondly

./jar.sh

You have two options. The first is ./run.sh , the second is java -jar gala.jar

